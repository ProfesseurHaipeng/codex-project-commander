interface:
  display_name: "Codex 我的总指挥"
  short_description: "建立Codex组织架构，让不同任务窗口各自负责不同成果"
  default_prompt: "使用 $project-commander-zh 为当前Codex项目建立组织架构和不同员工岗位，创建任务账本，并以中等模式持续派工。"

policy:
  allow_implicit_invocation: true
