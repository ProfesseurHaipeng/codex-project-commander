interface:
  display_name: "Project Commander"
  short_description: "Continuously route Codex tasks with a token-aware ledger"
  default_prompt: "Use $project-commander to organize this Codex project, create its task ledger, continuously reassign completed employees, and run in Balanced mode."

policy:
  allow_implicit_invocation: true
