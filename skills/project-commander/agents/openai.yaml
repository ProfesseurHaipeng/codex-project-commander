interface:
  display_name: "Project Commander"
  short_description: "Build a Codex organization with distinct task-window roles"
  default_prompt: "Use $project-commander to build this Codex project's organization chart, assign distinct task-window roles, create its task ledger, and run continuous dispatch in Balanced mode."

policy:
  allow_implicit_invocation: true
