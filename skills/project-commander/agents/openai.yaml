interface:
  display_name: "Project Commander"
  short_description: "Build governed Codex task-window organizations"
  default_prompt: "Use $project-commander to build this Codex project's organization chart, assign distinct task-window roles, create its task ledger, and run continuous dispatch in Balanced mode. Use the optional Three Departments and Six Ministries profile only when requested or justified by project risk."

policy:
  allow_implicit_invocation: true
