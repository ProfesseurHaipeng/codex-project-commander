interface:
  display_name: "Project Commander"
  short_description: "Build Codex task teams with completion monitoring"
  default_prompt: "Use $project-commander to build this Codex project's organization and ledger, run Balanced continuous dispatch, and create one heartbeat that validates completion, re-dispatches ready work, audits apparently idle employees, and lets them rest when no real work exists."

policy:
  allow_implicit_invocation: true
