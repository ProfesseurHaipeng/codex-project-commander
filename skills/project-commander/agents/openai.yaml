interface:
  display_name: "Project Commander"
  short_description: "Organize Codex projects into task-window workforces"
  default_prompt: "Use $project-commander to inspect this Codex project, organize employee task windows, assign model and reasoning profiles, and pin the commander."

policy:
  allow_implicit_invocation: true
